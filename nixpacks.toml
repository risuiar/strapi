[phases.setup]
nixpkgs = ["openssl", "cacert"]

[variables]
YARN_CACHE_FOLDER = "/tmp/.yarn-cache"
SWC_BINARY_PATH = ""

[phases.install]
cmds = ["yarn install --frozen-lockfile --prefer-offline false --cache-folder /tmp/.yarn-cache"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start"
